// import { countries } from "@/data/systemVariables";
// import { db } from "@/lib/firebase/client";
// import { doc, updateDoc } from "firebase/firestore";

export default async function Playground() {
  // const docRef = doc(db, "system_variables", "t4LXcJ1jwZK4eH4keyIO");
  // const res = await updateDoc(docRef, { countries: countries });
  // console.log(res);

  return <></>;
}
